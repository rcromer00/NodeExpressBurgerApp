{{!--  Setup the `index.handlebars` to have the template that Handlebars can render onto. --}}
{{!-- Create a button in `index.handlebars` that will submit the user input into the database. --}}
<head>
<link rel="preconnect" href="https://fonts.gstatic.com">
<link href="https://fonts.googleapis.com/css2?family=Exo:wght@200&display=swap" rel="stylesheet">
</head>

<h1 class="title">Eat Da Burger</h1>
<div id="burger_nameDiv">
    <input class="form-control" type="text" id="burger_name">
</div>
<div id="submitBtn">
    <button class="submitBtn" id="submit">Submit</button>
</div>


<div class="container" id="burgerList">
    <div class="row no-gutters align-self-center justify-content-between">
        <div class="col-md-6">
        <h3>Burger List</h3>
        <ul>
    {{#each burgers}}
        {{#unless this.devoured}}
        <li> {{this.burger_name}} <button class="devour" data-id={{this.id}} >Devour Now</button></li>
        {{/unless}}
    {{/each}}
    </ul>
        </div>
    


    <div class="col-md-6">
            <h3>Devoured Burger List</h3>
            <ul>
        {{#each burgers}}
            {{#if this.devoured}}
            <li> {{this.burger_name}}</li>
            {{!-- {{> burgers/burgerBlock devoured=1}} --}}
            {{/if}}
        {{/each}}
        </ul>
    </div> 
    </div>
</div>

    <h3 class="title-text">Create your Burger Below!</h3>